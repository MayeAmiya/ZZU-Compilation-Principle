
#include "includetest"[unfind]

static int test;

#include <string>

int main()
{
    std::string line;
    std::string input;

    std::cin >> line ;
    std::cout << line << endl;
    return 0;
}

#include "includetest"[unfind]

static int test;

#include <string>

int main()
{
    std::string line;
    std::string input;

    std::cin >> line ;
    std::cout << line << endl;
    return 0;
}

#include "includetest"[unfind]

static int test;

#include <string>

int main()
{
    std::string line;
    std::string input;

    std::cin >> line ;
    std::cout << line << endl;
    return 0;
}

#include "includetest"[unfind]

static int test;

#include <string>

int main()
{
    std::string line;
    std::string input;

    std::cin >> line ;
    std::cout << line << endl;
    return 0;
}

#include "includetest"[unfind]

static int test;

#include <string>

int main()
{
    std::string line;
    std::string input;

    std::cin >> line ;
    std::cout << line << endl;
    return 0;
}

#include "includetest"[unfind]

static int test;

#include <string>

int main()
{
    std::string line;
    std::string input;

    std::cin >> line ;
    std::cout << line << endl;
    return 0;
}
