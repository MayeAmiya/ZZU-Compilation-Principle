#include "include.cpptest"

main()
{
    let string line = null;
    let string input = null;
    let vector<int> list = null;
    let int a = null;
    let int b = null;

    list.push_back(0);
    istream::cin >> a >> b;

    if(a>b){
        list.clear();
        ostream::cout << abs(list[0]).string(a+b) << endl;
    }else{
        ostream::cout << full(string(a*b))  << endl;
    }

    if(a||b){
        ostream::cout << string(a+(a+b)) << endl;
    }
    return 0;
}
